[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# replace STM32F103C8 with your chip as listed in `probe-rs chip list`
runner = "arm-none-eabi-objcopy -O binary"

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "trace"
